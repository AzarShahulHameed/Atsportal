NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# Only needed in production, when this app's public pages are reverse-proxied
# under a sub-path on another domain (see cat-cons.com's next.config.js).
# Set to this app's own real deployed URL, e.g. https://atsportal.vercel.app
ASSET_PREFIX=
